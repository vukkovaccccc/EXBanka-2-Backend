version: "2"

run:
  timeout: 5m
  go: "1.24"

linters:
  default: none
  enable:
    - errcheck
    - govet
    - staticcheck
    - ineffassign
    - unused
  exclusions:
    generated: lax
    paths:
      - proto
      - docs/swagger
      - services/.*/mocks

formatters:
  enable:
    - gofmt
